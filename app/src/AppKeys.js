// These keys are all "dummy" on the public repo.
// For production builds, use a different set of keys
// Once set, never change these keys or you'll break future builds.
export default {
    userdata: "dummy"
}
